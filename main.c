#include <stdio.h>
#include <math.h>
#include <string.h>
#include "algoritmia.h"
int main(char entrada[1]){
  /*  if (entrada[0] == "s" ){
        return 0;
    }
    else{
      main(entrada);
    }
    */
   Menu();
   Registrar_montos();
   Montos_per_ciudad();
    return 0;
}